{
  "name": "ha-bereginya-aura",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit --pretty false",
    "build:frontend": "node scripts/build-frontend.mjs",
    "check:frontend-artifacts": "node scripts/build-frontend.mjs --check"
  },
  "dependencies": {
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.3"
  }
}
